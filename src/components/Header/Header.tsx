import { FC } from "react";
import './Header.css'; 

export const Header: FC = () => {
    return(
        <header>
            <div>
                <h1>devjobs</h1>
            </div>
        </header>
    )
}